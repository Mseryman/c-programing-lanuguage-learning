#include <stdio.h>

int main() {
    int a; // defines a variable a of type int
    scanf("%d", &a); // get the input value from the user and assign that value to a
    printf("%d", a); // print the value of a which is received from the user as input
    return 0;
}